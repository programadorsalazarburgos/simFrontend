import { MarkType } from 'prosemirror-model';
import { EditorState, Transaction } from 'prosemirror-state';
export declare const applyMark: (type: MarkType, attrs?: Record<string, any>) => (state: EditorState, dispatch?: (tr: Transaction) => void) => boolean;
