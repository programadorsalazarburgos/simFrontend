import { EditorState } from 'prosemirror-state';
import { Dispatch } from './types';
declare type Execute = (state: EditorState, dispatch?: Dispatch) => boolean;
declare type Name = 'text_color' | 'text_background_color';
declare class TextColor {
    name: Name;
    constructor(name: Name);
    execute(attrs: {}): Execute;
    isActive(state: EditorState): boolean;
    getActiveColors(state: EditorState): string[];
    remove(state: EditorState, dispatch: Dispatch): boolean;
    canExecute(state: EditorState): boolean;
}
export default TextColor;
