import { ElementRef, OnDestroy } from '@angular/core';
import { AbstractControl, FormGroup } from '@angular/forms';
import { NgxEditorService } from '../../../editor.service';
import { SharedService } from '../../../services/shared/shared.service';
export declare class ImageComponent implements OnDestroy {
    private el;
    private ngxeService;
    private sharedService;
    showPopup: boolean;
    isActive: boolean;
    private pluginUpdateSubscription;
    form: FormGroup;
    private editorView;
    name: string;
    constructor(el: ElementRef, ngxeService: NgxEditorService, sharedService: SharedService);
    get valid(): boolean;
    get icon(): string;
    get src(): AbstractControl;
    onDocumentClick(e: MouseEvent): void;
    getLabel(key: string): string;
    private hideForm;
    onMouseDown(e: MouseEvent): void;
    private fillForm;
    private update;
    insertLink(e: MouseEvent): void;
    ngOnDestroy(): void;
}
