import { ElementRef, OnDestroy, OnInit, Renderer2 } from '@angular/core';
import { Mark } from 'prosemirror-model';
import { SharedService } from '../../services/shared/shared.service';
export declare class BubbleComponent implements OnInit, OnDestroy {
    private sharedService;
    private el;
    private renderer;
    private view;
    activeLinkItem: Mark;
    private pluginUpdateSubscription;
    constructor(sharedService: SharedService, el: ElementRef, renderer: Renderer2);
    private setDomPosition;
    private showBubble;
    private hideBubble;
    removeLink(): void;
    private update;
    ngOnInit(): void;
    ngOnDestroy(): void;
}
